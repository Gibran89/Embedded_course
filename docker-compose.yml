version: '3'
services:
  embedded_course_dev:
    build: .
    volumes:
      - .:/workspace
      - ./output:/build/output
    user: "${UID:-1000}:${GID:-1000}"  # This will run the container as your user
    working_dir: /workspace
